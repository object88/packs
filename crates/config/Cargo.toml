[package]
name = "config"
edition.workspace = true
version.workspace = true

[lib]

[dependencies]
packs_logging = { path = "../packs_logging" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.149" }
tracing.workspace = true
