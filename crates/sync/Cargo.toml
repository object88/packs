[package]
name = "sync"
edition.workspace = true
version.workspace = true

[features]
hotpath = ["hotpath/hotpath"]

[lib]

[dependencies]
hotpath = { version = "0.14.0", features = ["hotpath", "tokio"] }
tokio.workspace = true
